@model IEnumerable<Programacion3.Models.cargos>

@{
    ViewBag.Title = "Cargo";
}

<h2 class="tit">Cargo</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Codigo")
        </th>
        <th>
            @Html.DisplayName("Cargo")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigoCargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cargo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_cargos }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_cargos })
        </td>
    </tr>
}

</table>
